<div class="top-bar">
	<h1><%= @project.title%></h1>
</div>

<div class="table">
  <table class="listing form" cellpadding="0" cellspacing="0">
    <tr>
			<th colspan="2">Project</th>
		</tr>
					<tr>
				<td><strong>title</strong></td>
				<td><%= @project.title %></td>
			</tr>
					<tr>
				<td><strong>description</strong></td>
				<td><%= @project.description %></td>
			</tr>
					<tr>
				<td><strong>mail</strong></td>
				<td><%= @project.mail %></td>
			</tr>
					<tr>
				<td><strong>created_at</strong></td>
				<td><%= @project.created_at %></td>
			</tr>
					<tr>
				<td><strong>manager_ids</strong></td>
				<td><%= @project.manager_ids %></td>
			</tr>
					<tr>
				<td><strong>designer_ids</strong></td>
				<td><%= @project.designer_ids %></td>
			</tr>
					<tr>
				<td><strong>developer_ids</strong></td>
				<td><%= @project.developer_ids %></td>
			</tr>
			</table>
</div>

<div class="actions">
	<%= link_to 'List', admin_projects_path, :class => "action list" %> |
	<%= link_to 'Edit', edit_admin_project_path(@project), :class => "action edit" %> |
	<%= link_to 'Destroy', admin_project_path(@project), :method => :delete, :confirm => "Are you sure?", :class => "action destroy" %>
</div>
